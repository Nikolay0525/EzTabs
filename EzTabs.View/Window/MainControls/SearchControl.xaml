<UserControl x:Class="EzTabs.View.Window.MainControls.SearchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EzTabs.View.Window.MainControls"
             xmlns:vm="clr-namespace:EzTabs.ViewModel.MainControlsViewModels;assembly=EzTabs.ViewModel"
             mc:Ignorable="d" 
             FontFamily="Inter"
             FontSize="24"
             d:DesignHeight="540" d:DesignWidth="960"
             Background="#93B5C6">
    <UserControl.Resources x:Uid="CommonButton">

        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="IconButton" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:SearchControlViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="56*"/>
            <RowDefinition Height="485*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="161*"/>
            <ColumnDefinition Width="79*"/>
        </Grid.ColumnDefinitions>

        <Button x:Name="MenuButton"
                Background="Transparent"
                BorderBrush="Black"
                BorderThickness="2"
                Margin="568,-86,-374,96" Grid.Column="1">
            <Image Stretch="Fill" Source="/Resources/Images/Menu.png"/>
        </Button>

        <Button x:Name="NotificationButton"
                BorderBrush="#00000000"
                BorderThickness="2"
                Margin="502,-48,-308,58" Foreground="Black" Background="#00DDDDDD" Grid.Column="1">
            <Path Data="M10.5,2v2.19531c-2.58682,0.66678 -4.5,3.00949 -4.5,5.80469v6l-2,2v1h16v-1l-2,-2v-6c0,-2.7952 -1.91318,-5.1379 -4.5,-5.80469v-2.19531zM10,20c0,1.1 0.9,2 2,2c1.1,0 2,-0.9 2,-2z" Fill="Black" Stretch="Fill" Width="21"/>
        </Button>

        <Grid Grid.Row="1">
            <ListBox x:Name="TabList"
                Background="White"
                BorderBrush="Black"
                BorderThickness="2"
                VerticalAlignment="Center"
                Height="484" HorizontalAlignment="Center" Width="640">
                <Button/>

            </ListBox>
        </Grid>
        <Grid Grid.Column="1" Grid.Row="1">
            <Button x:Name="LoadTabButton"
                Width="250"
                Content="Edit Tab"
                Background="White"
                BorderBrush="Black"
                BorderThickness="2"
                FontSize="36"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Margin="0,270,0,0" Height="50"/>
            <Button x:Name="CreateTabButton"
                Width="250"
                Content="Create Tab"
                Background="White"
                BorderBrush="Black"
                BorderThickness="2"
                FontSize="36"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Margin="0,128,0,0" Height="50"/>
        </Grid>
        <Grid Margin="0,0,0,484" Grid.RowSpan="2">
            <Button x:Name="FilterButton"
                Width="88"
                Height="56"
                Content="Filter"
                Background="White"
                BorderBrush="Black"
                BorderThickness="2"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"/>
            <TextBox x:Name="SearchTextBox" 
                Background="White"
                BorderBrush="Black"
                BorderThickness="2"
                Text="Search..."
                Height="56" 
                VerticalAlignment="Center"
                Margin="88,0,0,0" HorizontalAlignment="Left" Width="556"/>
        </Grid>
        <Grid Grid.Column="1" Margin="0,0,0,484" Grid.RowSpan="2">
            <TextBlock x:Name="UsernameTextBlock"
                Width="280"
                Foreground="Black"
                TextAlignment="Center"
                HorizontalAlignment="Center" 
                VerticalAlignment="Top"
                Margin="0,10,0,0" Height="29" Text="Nik0_ola"/>
        </Grid>
    </Grid>
</UserControl>
